{
  "name": "ocr-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt",
    "dev": ". .venv/bin/activate && python app.py"
  }
}
