# ==============================================================================
# MoneyMate - Production Environment Variables
# Copy to /opt/moneymate/.env and fill in your values
# Usage: cp deploy/.env.example .env && nano .env
# ==============================================================================

# ---- Domain & SSL ----
DOMAIN=moneymate.example.com
SSL_EMAIL=admin@example.com

# ---- Node Environment ----
NODE_ENV=production
PORT=3000

# ---- Database (PostgreSQL) ----
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE
POSTGRES_DB=moneymate
DATABASE_URL=postgresql://postgres:CHANGE_ME_STRONG_PASSWORD_HERE@postgres:5432/moneymate

# ---- Redis ----
REDIS_URL=redis://redis:6379
REDIS_HOST=redis
REDIS_PORT=6379

# ---- JWT Authentication ----
JWT_SECRET=CHANGE_ME_RANDOM_64_CHAR_STRING_HERE
JWT_EXPIRES_IN=7d

# ---- API URLs ----
VITE_API_URL=https://moneymate.example.com/api
API_EXTERNAL_URL=http://moneymate-server:3000

# ---- OCR Worker ----
OCR_WORKER_URL=http://ocr-worker:8080

# ---- Backup (optional) ----
GCS_BACKUP_BUCKET=

# ---- Monitoring (optional) ----
# Discord/Slack webhook for health check alerts
MONEYMATE_DOMAIN=moneymate.example.com
ALERT_WEBHOOK_URL=
